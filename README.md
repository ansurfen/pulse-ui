# PulseUI

Gamified React Native design system monorepo with a dedicated Expo playground.

## Packages

- `packages/core`: tokens and theme helpers
- `packages/ui`: reusable UI components
- `packages/motion`: shared animation presets
- `apps/playground`: Expo preview app with component gallery

## Getting Started

```bash
pnpm install
pnpm web
```

